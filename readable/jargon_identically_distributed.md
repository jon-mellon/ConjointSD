# Identically distributed

A sequence is identically distributed if each element has the same distribution. That means each draw looks statistically like the others, even if they are different random draws.
