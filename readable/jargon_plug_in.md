# Plug-in

Lean entrypoint: [ConjointSD.lean](../ConjointSD.lean)

A plug-in estimate takes an estimator of a parameter and plugs it into a formula that normally uses the true parameter. It is the standard way to turn a population formula into an estimator.
