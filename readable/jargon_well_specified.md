# Well-specified

A model is well-specified if it exactly matches the true causal target it is meant to represent. In this project, that means the linear model equals the causal score function for all profiles.
